from setuptools import find_packages, setup

setup(
    name='resolve_path',
    version='0.1',
    packages=find_packages(),
    description='A package to resolve the changing local to global directory adressing between .ipynb and .py files in the same repo.',
    author='Eduardo Eugênio',
)
